// Created by kl2dfg (processFunction)
{
  "objectType" : "Func",
  "metadata" : {
    "uiTooltip" : "returns true if this controller is valid\n\n Supported by LeapInteractionBox"
    },
  "title" : "Valid[]",
  "ports" : [
    {
      "objectType" : "Port",
      "nodePortType" : "Out",
      "name" : "this",
      "execPortType" : "In",
      "typeSpec" : "LeapInteractionBox[]"
      },
    {
      "objectType" : "Port",
      "nodePortType" : "In",
      "name" : "result",
      "execPortType" : "Out",
      "typeSpec" : "Boolean[]"
      }
    ],
  "extDeps" : {
    "LeapMotion" : "*"
    },
  "presetGUID" : "BA21C0A89D980591500E3A0CDF29AEAF",
  "code" : "require LeapMotion;

operator LeapMotion_LeapInteractionBox_valid_task<<<index>>>(
  io Boolean[] result
, in LeapInteractionBox[] this
) {
  result[index] = this[index < this.size() ? index : this.size() - 1].valid();
}

dfgEntry {
  if( this.size() == 0) return;
  Size task_size = this.size();
  result.resize(task_size);
  if(task_size < 10000 /* pex margin */) {
    for(Size index = 0; index < task_size; index++) {
      LeapMotion_LeapInteractionBox_valid_task(index, result, this);
    }
  } else {
    LeapMotion_LeapInteractionBox_valid_task<<<task_size>>>(result, this);
  }
}
"
  }